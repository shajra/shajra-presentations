If you have a JDK installed, you can compile this project with

```
$ ./sbt tut
```

and then load up the resultant HTML presentation in a browser.

```
$ $YOUR_FAV_BROWER target/scala-2.10/tut/scala-expressionproblem.html
```
